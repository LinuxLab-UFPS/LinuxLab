# Dependencias
node_modules

# Logs
logs
*.log

# Next.js
.next/
out/

# Build
*.tsbuildinfo

# Variables de entorno
.env
.env.*
!.env.example
.env*.local

# Videos (frontend)
frontend/public/temario/**/*.mp4
frontend/public/temario/**/*.webm
frontend/public/temario/**/*.mov

# Prisma generated
/generated/prisma
backend/generated/prisma

# Docker
.env.production
.env.staging

# Sistema
.DS_Store

# IDE
.idea/
.vscode/

# Notas de trabajo, no van al repositorio
NOTAS-LOCALES.md

# Respaldos de base de datos
scripts/backups/

# Artefactos del despliegue
imagenes.tar.gz

# Configuracion local del despliegue (credenciales del servidor)
deploy/.deploy.env

# Claves privadas (nunca al repo)
*.pem
*_rsa
*_ed25519
1152186
# Copias de la base con intentos de estudiantes
backend/prisma/respaldo/

# Compose de desarrollo local. Se queda fuera del repo porque cada maquina
# apunta a un backend distinto: NEXT_PUBLIC_BACKEND_URL se incrusta en el
# bundle del navegador y en local tiene que ser localhost, no el nombre de la
# red de Docker ni produccion. El despliegue real no lo usa, va con
# deploy/compose.podman.yml.
docker-compose.yml
