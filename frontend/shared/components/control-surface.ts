/**
 * La superficie de los controles de filtrado (busqueda y selects): blanca en
 * claro y gris en oscuro. Es la misma receta del boton `outline` de la app,
 * para que la barra de busqueda y los filtros se lean como la misma familia
 * que los botones que los acompanan. Sin ella iban transparentes sobre el
 * fondo de la pagina y con el borde tenue de las tablas.
 */
export const CONTROL_SURFACE = "bg-background dark:bg-input/30"
